# YOLO11 基本設定ファイル（Mediumモデル仕様）
processing:
  depth_estimation:
    enabled: false
  detection:
    confidence_threshold: 0.3
    iou_threshold: 0.45
    save_visualizations: true          # 🆕 追加
    save_detection_frames: true        # 🆕 追加
    max_detections: 1000
  tile_inference:
    enabled: false
    tile_size: [640, 640]
    overlap_ratio: 0.2

# 🆕 新セクション追加
visualization:
  save_visualizations: true
  save_annotated_frames: true
  output_format: "jpg"
  create_summary_plots: true
  save_intermediate_results: true

models:
  detection: "yolo11m.pt"        # 自動ダウンロード対応
  pose: "yolo11m-pose.pt"       # 自動ダウンロード対応
  depth: "models/depth/DPT_Large.pt"

  # 利用可能なモデル（コメントアウト）
  # Nano (高速):     yolo11n.pt, yolo11n-pose.pt
  # Small (バランス): yolo11s.pt, yolo11s-pose.pt  
  # Medium (高精度):  yolo11m.pt, yolo11m-pose.pt ← 現在使用中
  # Large (より高精度): yolo11l.pt, yolo11l-pose.pt
  # XLarge (最高精度): yolo11x.pt, yolo11x-pose.pt

video_dir: "videos"
output_dir: "outputs"
